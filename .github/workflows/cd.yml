name: Spring Boot CD

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4


      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Login to Azure Container Registry
        run: az acr login --name myprojectacr123

      - name: Build Docker image
        run: docker build -t myprojectacr123.azurecr.io/hello-api:latest ./app
        
      - name: Push Docker image to ACR
        run: docker push myprojectacr123.azurecr.io/hello-api:latest